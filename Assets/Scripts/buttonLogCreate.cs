using UnityEngine;
using System.Collections;

public class buttonLogCreate : MonoBehaviour
{


    public GameObject textBox;

	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
/*
	    if (Input.anyKeyDown)
	    {
	        GameObject clone;
	        clone = Instantiate(textBox, transform.position, transform.rotation)as GameObject;
            clone.transform.SetParent(transform, false);

	    }
	    */
	
	}
}
